#include<stdio.h>
#include<stdlib.h>
#include<unistd.h>
#include<sys/types.h>
#include<sys/wait.h>
int main(){
   pid_t P2,P3,P4,P5,P6,P7;
   printf("Parent process:P1 \t PID=%d \n",getpid());
   P2=fork();
   if(P2<0){
      perror("P2 fork failed");
   }
   else if(P2==0){
      printf("P2:child of P1\t PID=%d \t ParentID=%d\n",getpid(),getppid());
   }
   else{
      wait(NULL);
      P3=fork();
      if(P3<0){
	 perror("P3 fork failed");
      }
      else if (P3==0){
	 printf("P3:child of P1\t PID=%d \t ParentId=%d\n",getpid(),getppid());
         P6=fork();
	 if(P6<0){
	    perror("P6 fork failed");
	 }
	 else if(P6==0){
	    printf("P6:child of P3 \t PID=%d\t ParentId=%d\n",getpid(),getppid());
	 }
	 else{
	    wait(NULL);
	    P7=fork();
	    if(P7<0){
	       perror("P7 fork failed");
	       return 1;
	    }
	    else if(P7==0){
	       printf("P7:child of P3\t PID=%d \t ParentId=%d\n",getpid(),getppid());
	    }
	    else{
	       wait(NULL);
	       return 1;
	    }
	  }
      }
       else{
	  wait(NULL);
	  P4=fork();
	  if(P4<0){
	     perror("P4 fork failed");
	  }
	  else if(P4==0){
	     printf("P4:child of P1\t PID=%d \t Parentid=%d\n",getpid(),getppid());
	  }
	  else{
	     wait(NULL);
	     P5=fork();
	     if(P5<0)
	     {
		printf("P5 fork failed");
	     }
	     else if(P5==0){
		printf("P5:child of P1\t PID=%d\t ParentId=%d\n",getpid(),getppid());
	     }
	     else{
		wait(NULL);
		return 0;
	     }
	  }
       }
      }
   }

